export enum KEY_CODE {
  first = 49,
  second = 50,
  third = 51,
  forth = 52,
  fifth = 53,
  back = 8,
  confirm = 32
}

export enum sections {
  questions = 'question',
  answers = 'answers',
  statistic = 'statistic'
}
